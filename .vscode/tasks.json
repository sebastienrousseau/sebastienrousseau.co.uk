{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "site-production",
            "type": "shell",
            "command": "JEKYLL_ENV=production bundle exec jekyll serve --watch --trace",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}